# Z2H Explore MCP

**Repo:** https://github.com/ram-amit/z2h-explore-mcp (public - no access request needed)

MCP server for creating and managing **looks** and **dashboards** in [campaign-explore](https://bigbrain.me/bigbrain-vibe/campaign-explore) on bigbrain.me. Intended as the Z2H replacement for Looker MCP after Looker sunset.

## Install (new users)

Pick one. Requires **Python 3.10+** (macOS Xcode `python3` is often 3.9; `brew install python` fixes that). All paths create a venv, write `.env`, and merge `~/.cursor/mcp.json` with your personal campaign-explore folder (`git config user.name`). **Restart Cursor** when done.

### Option A: one-liner (recommended for Slack)

Copy/paste exactly:

```bash
git clone https://github.com/ram-amit/z2h-explore-mcp.git ~/z2h-explore-mcp && cd ~/z2h-explore-mcp && ./install-z2h-explore-mcp.sh --dir .
```

Change `~/z2h-explore-mcp` only if you want a different install folder.

### Option B: curl bootstrap

GitHub raw may serve Windows line endings; strip them before bash:

```bash
curl -fsSL https://raw.githubusercontent.com/ram-amit/z2h-explore-mcp/main/install-z2h-explore-mcp.sh | tr -d '\r' | bash
```

**Paste in Slack (new install):**

> **campaign-explore MCP for Cursor**
>
> create/manage looks + dashboards in campaign-explore from chat. saves to your personal folder (`git config user.name`).
>
> **paste in Terminal (copy all 1 line):**
> ```
> git clone https://github.com/ram-amit/z2h-explore-mcp.git ~/z2h-explore-mcp && cd ~/z2h-explore-mcp && ./install-z2h-explore-mcp.sh --dir .
> ```
> then **restart Cursor** and test: `list explores in campaign-explore`
>
> needs Python 3.10+. if install fails on `mcp`, run `brew install python` then `cd ~/z2h-explore-mcp && rm -rf venv && ./install-z2h-explore-mcp.sh --dir .`
>
> repo: https://github.com/ram-amit/z2h-explore-mcp

### Option C: clone then install (SSH)

```bash
git clone git@github.com:ram-amit/z2h-explore-mcp.git ~/z2h-explore-mcp
cd ~/z2h-explore-mcp
./install-z2h-explore-mcp.sh --dir .
```

### Option D: manual (developers)

```bash
cd /path/to/z2h-explore-mcp
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # set Z2H_EXPLORE_PERSONAL_FOLDER to your display name
```

Add to `~/.cursor/mcp.json`:

```json
"z2h-explore": {
  "command": "/path/to/z2h-explore-mcp/venv/bin/python3",
  "args": ["/path/to/z2h-explore-mcp/server.py"],
  "cwd": "/path/to/z2h-explore-mcp",
  "env": {
    "Z2H_EXPLORE_PERSONAL_FOLDER": "Your Display Name",
    "Z2H_EXPLORE_DEFAULT_STORAGE": "personal"
  }
}
```

Or run `./install-z2h-explore-mcp.sh --dir .` to generate the block automatically.

## After install

1. Restart Cursor
2. Try: `list explores in campaign-explore`
3. Optional follow-up prompt: `CURSOR_SETUP_PROMPT.md` in the install folder (generated by installer)

**Storage default:** looks and dashboards save under `looks/<your name>/` and `dashboards/<your name>/`, then pin to the matching `index.json`. Pass `shared=true` on create only when you explicitly want shared/global storage.

## Upgrade (existing install)

```bash
cd /path/to/z2h-explore-mcp
git pull
./migrate-personal-storage.sh
```

Or re-run the full installer (also refreshes venv + `mcp.json`):

```bash
cd /path/to/z2h-explore-mcp && ./install-z2h-explore-mcp.sh --dir .
```

**Slack one-liner (upgrade):**

> campaign-explore MCP upgrade - run locally, then restart Cursor:
> `cd /path/to/z2h-explore-mcp && git pull && ./migrate-personal-storage.sh`

## Ship updates to the team

1. Commit and push to `main` on https://github.com/ram-amit/z2h-explore-mcp
2. Teammates run `git pull` + `./migrate-personal-storage.sh` (or re-run `./install-z2h-explore-mcp.sh --dir .`)
3. Restart Cursor

| Env / flag | Purpose |
|------------|---------|
| `--dir` / `Z2H_EXPLORE_MCP_DIR` | Install path (your choice; fallback `~/Development/z2h-explore-mcp` or `~/z2h-explore-mcp`) |
| `--repo-url` / `Z2H_EXPLORE_MCP_REPO` | Git clone URL (default `git@github.com:ram-amit/z2h-explore-mcp.git`) |

## Tools

| Tool | Purpose |
|------|---------|
| `list_explores` | Campaign Monitoring, Advanced Analytics, LinkedIn Habu |
| `get_dimensions` / `get_measures` / `get_parameters` | Field catalog (CM full; others via sample looks) |
| `get_field_info` | Single field metadata |
| `list_looks` / `get_look` / `create_look` / `update_look` / `delete_look` | Saved views |
| `list_dashboards` / `get_dashboard` / `create_dashboard` / `update_dashboard` / `add_dashboard_tile` / `delete_dashboard` | Dashboards |
| `get_z2h_url` | Build `bigbrain.me/bigbrain-vibe/campaign-explore?...` link |
| `migrate_from_looker` | One-shot Looker to Z2H migration |

## API

Backed by `marketing-foundations`:

- Base: `https://marketing-foundations.bigbrain.me/api/tools/v1/looker-z2h`
- Storage: `GET/PUT/DELETE /storage/file`, `GET /storage/files?prefix=`
- Migration: `POST /migrate/look`, `POST /migrate/dashboard`

**Storage key encoding:** keys returned by `list_*` may contain literal `%20`. Pass those keys as-is to `get_*` / `update_*` / `delete_*`.

## URLs

- Dashboard: `https://bigbrain.me/bigbrain-vibe/campaign-explore?dashboard=9490`
- Look: `https://bigbrain.me/bigbrain-vibe/campaign-explore?look=<uuid>`

## Notes

- Field catalog is pulled from the deployed `mf-campaign-explore` schema chunk (cached in `~/.cache/z2h-explore-mcp/`).
- `run_query` / SQL preview are not in v1; query execution is client-side in campaign-explore.
- Writes use the same unauthenticated storage API as the frontend; if PUT fails, set `BIGBRAIN_AUTH_TOKEN` or connect via VPN.
